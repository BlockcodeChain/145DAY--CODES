// Take 20 elements from user input and find its sum with the help of an array.
#include<iostream>
using namespace std;
 int mai(){
    int num;
    cout<<"Enter number of element in array :";
    cin>>num;
 int int array[30];
 for(int i=0;i<num;i++){
    cout<<"Enter "<<i+1<<" element:";
    cin>>array[i];
 }
for(int i=0;i<num;i++){
    sum+=array[i]
} 
cout<<"Sum of "<<num<<" element :"<<sum<<endl;
}
